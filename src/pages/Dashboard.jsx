import React from 'react';
import DashboardFeature from '../features/dashboard/DashboardFeature';

export default function Dashboard() {
  // A página atua apenas como container para a feature
  return <DashboardFeature />;
}